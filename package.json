{
  "main": "index.js",
  "dependencies": {
    "@atomist/skill": "0.1.1-21"
  },
  "scripts": {
    "skill": "atm-skill generate && atm-skill bundle --minify --source-map --file=index.js && atm-skill package"
  }
}
